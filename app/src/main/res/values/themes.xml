<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SETP" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primaryBlue</item>
        <item name="colorPrimaryVariant">@color/primaryDarkBlue</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/accentBlue</item>
        <item name="colorSecondaryVariant">@color/accentBlue</item> <!-- Can be a darker shade of accentBlue if available -->
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@color/light_gray</item>
        <item name="android:textColorPrimary">@color/textColorPrimary</item>
        <item name="android:textColorSecondary">@color/textColorSecondary</item>
        <item name="dividerHorizontal">@color/dividerColor</item> <!-- For dividers -->
    </style>

    <style name="Theme.SETP" parent="Base.Theme.SETP" />

    <!-- Specific style for buttons -->
    <style name="App.Button.Blue" parent="Widget.Material3.Button.OutlinedButton">
        <item name="backgroundTint">@color/primaryBlue</item>
        <item name="android:textColor">@color/white</item>
        <item name="strokeColor">@color/primaryDarkBlue</item> <!-- Optional: for outlined button border -->
        <item name="rippleColor">@color/lightPrimaryBlue</item> <!-- Optional: for ripple effect -->
    </style>

</resources>